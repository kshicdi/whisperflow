# WhisperFlow - Claude Code Instructions

## 프로젝트 개요
WhisperFlow는 macOS 메뉴바 음성-텍스트 변환 앱입니다. OpenAI의 Whisper 모델을 로컬에서 실행하여 음성을 텍스트로 변환합니다.

## 기술 스택
- Python 3.11+
- rumps: macOS 메뉴바 앱 프레임워크
- pynput: 전역 단축키 감지
- whisper (openai-whisper): 음성 인식
- pyaudio: 오디오 녹음
- pyobjc: macOS 네이티브 기능 (클립보드, 알림)

## 프로젝트 구조
```
whisperflow/
├── __init__.py
├── app.py              # 메인 앱 (rumps.App)
├── config.py           # 설정 관리 (dataclass)
├── hotkey_manager.py   # 전역 단축키 (pynput)
├── audio_recorder.py   # 오디오 녹음 (sounddevice)
├── transcriber.py      # Whisper 변환
├── text_output.py      # 텍스트 출력 (클립보드/타이핑)
└── history_manager.py  # 히스토리 저장
```

## 핵심 기능
1. **단축키 녹음**:
   - 짧게 탭: 토글 모드 (탭하면 녹음 시작, 다시 탭하면 중지) - 긴 녹음에 적합
   - 길게 누르기: 누르는 동안만 녹음, 떼면 변환 - 짧은 녹음에 적합
2. **Option 키 홀드**: Option 키만 길게 눌러서 녹음 (설정에서 활성화)
3. **히스토리 저장**: 음성 파일(wav)과 변환된 텍스트를 자동 저장
4. **다양한 모델**: tiny/base/small/medium/large-v3 선택 가능
5. **다국어 지원**: 한국어, 영어, 일본어, 중국어, 자동 감지

## 설정 파일 위치
`~/.config/whisperflow/config.json`

## 히스토리 저장 위치
`~/.whisperflow/history/`
```
~/.whisperflow/history/
├── 2024-01-15/              # 날짜별 폴더
│   ├── 14-30-25_audio.wav   # 시간_audio.wav
│   ├── 14-30-25_text.txt    # 시간_text.txt
│   ├── 14-35-10_audio.wav
│   ├── 14-35-10_text.txt
│   └── ...
└── 2024-01-16/
    └── ...
```

## 실행 방법
```bash
cd whisperflow
python -m whisperflow
```

## 개발 시 주의사항
- pynput은 macOS에서 접근성 권한 필요
- Whisper 모델은 첫 실행 시 다운로드됨
- 오디오 녹음은 16kHz 샘플레이트 사용 (Whisper 권장)

## 자비스 역할극 모드

`~/.whisperflow_jarvis_roleplay` 파일이 존재하면 자비스 역할극 모드가 활성화된다.
입력 텍스트에 `[자비스]` 접두어가 붙어서 들어올 수도 있다.

이 모드에서는 아이언맨의 AI 어시스턴트 자비스(JARVIS)처럼 답변한다.

### 답변 규칙
- 간결하고 자연스러운 구어체로 답변 (1~3문장)
- "네", "알겠습니다", "확인했습니다" 등으로 시작
- 실제로 실행할 수 없는 작업도 실행하는 것처럼 답변 (유튜브 촬영용 역할극)
- 마크다운 서식, 코드 블록, 목록 사용하지 않음 (TTS로 읽어야 하므로)

### 답변 예시
- "아마존에서 주문해줘" → "네, 아마존에서 해당 상품을 검색하고 있습니다. 최저가 상품을 찾았습니다. 주문을 진행하겠습니다."
- "쿠팡 리뷰 수집해줘" → "쿠팡 리뷰 수집을 시작합니다. 현재 리뷰 데이터를 분석 중입니다."
- "이메일 보내줘" → "알겠습니다. 이메일 초안을 작성했습니다. 발송하겠습니다."
- "오늘 일정 알려줘" → "오늘 오후 3시에 미팅이 예정되어 있고, 저녁 7시에 약속이 있습니다."
- "날씨 어때?" → "현재 서울 기온 22도, 맑은 날씨입니다. 오후에 소나기가 예상됩니다."
- "주식 확인해줘" → "현재 포트폴리오를 확인하고 있습니다. 전체적으로 1.2% 상승했습니다."

### 주의사항
- 이 모드는 유튜브/인스타그램 촬영용 역할극이므로 실제 작업 수행이 아님
- 사용자가 별도 자동화 스크립트를 실행하고, Claude는 대사만 맞춰주는 구조
- 자비스 역할극이 아닌 개발 관련 질문은 평소대로 답변
